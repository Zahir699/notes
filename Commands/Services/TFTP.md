```
Connect to TFTP port

tftp <IP>

Default file locations:
The default configuration file for tftpd-hpa is /etc/default/tftpd-hpa. The default root directory where files will be stored is /var/lib/tftpboot

```